apiVersion: v1
kind: Service
metadata:
  name: devops-2-frontend-service
spec:
  type: NodePort
  selector:
    app: devops-2-frontend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30500
